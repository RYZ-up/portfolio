[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  # Enable Git LFS to download actual video files instead of pointers
  GIT_LFS_ENABLED = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
